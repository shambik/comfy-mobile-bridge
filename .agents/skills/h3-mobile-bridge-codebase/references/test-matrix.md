# Test matrix

| Change | Minimum proof |
|---|---|
| Config or paths | clone from another directory, missing config, invalid config, no parent traversal |
| Downloads | resume .part, wrong size, wrong SHA-256, valid existing file, repeat bootstrap |
| Queue or API | Python unit tests, SQLite preservation, health version |
| Workflow | pinned node commits and /object_info classes |
| Tailscale | local listeners, Serve JSON, no Funnel, generated hostname |
| Media | ffprobe streams plus full FFmpeg decode |
